# Optional: base URL of a CDN (e.g. Cloudflare R2 public bucket URL) serving the
# scroll-animation frames from public/9mb/. Leave unset to serve frames locally.
# Example: https://pub-xxxxxxxxxxxxxxxx.r2.dev
NEXT_PUBLIC_ASSET_CDN_URL=
